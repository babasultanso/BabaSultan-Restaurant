# BABA Sultan Live — تقرير الفحص والإصلاح الشامل

**التاريخ:** 2026-09-24  
**النطاق:** الواجهة React/Vite، الخادم Express، Firebase Auth/Firestore/Storage، العزل بين الفروع، الصلاحيات، API، البيانات المالية والمخزون، الترجمة، إعدادات النشر، والاختبارات الساكنة.

## 1. نتيجة العمل
تمت مراجعة المشروع كاملًا وعدم التوقف عند أول خطأ. تم إصلاح مجموعة من العيوب التي ظهرت عبر طبقات مختلفة، ثم أُعيد الفحص بعد التعديلات.

### أهم الإصلاحات
- تشديد CORS وحدود جسم الطلب ومحدد المعدل في API، مع 404 موحد لمسارات `/api/*` وحاجز أخطاء مركزي دون تسريب stack traces.
- إضافة مسار Vercel catch-all للـAPI (`api/[...path].ts`) وإصلاح إعدادات `vercel.json` لتعمل SPA مع API.
- نقل تعديل الدور/الحالة للجهة الخادمية في مسار إداري ذري Transaction بدل الكتابة الأمنية المباشرة من العميل.
- جعل إنشاء المستخدم الإداري آمنًا ضد overwrite، مع تنظيف Auth عند فشل إنشاء ملف Firestore.
- جعل تحويلات الفروع خادمية ومرتبطة بعنصر/موظف حقيقي وتكلفة حقيقية بدل `itemId` وهمي وتكلفة ثابتة.
- منع الحسابات الموقوفة من المسارات التشغيلية عبر طبقة المصادقة وقواعد Firestore/Storage.
- توحيد تعريف HQ/Owner مع دعم Admin المعلّم `isHQ=true` أو `branchId='all'`، وإزالة التعارض بين الواجهة والقواعد.
- إضافة نطاق قراءة صريح (`getEffectiveBranchScope`) يفصل بين القراءة متعددة الفروع المسموحة للـHQ والعمليات التي تتطلب فرعًا محددًا.
- تقييد استعلامات المنتجات، العملاء، الموظفين، الموارد البشرية، المحاسبة، الوصفات، مناطق التوصيل، الإشعارات والتحويلات حسب الفرع لغير HQ، مع دعم قراءة HQ عند الحاجة.
- إصلاح حالات stale state عند وصول snapshot فارغ في عدة شاشات realtime.
- إضافة/تصحيح فهارس Firestore اللازمة للاستعلامات المركبة.
- إزالة كتابات العميل المباشرة إلى المجموعات المالية authoritative، وجعل التدفقات المالية الحساسة خادمية.
- توحيد نموذج بيانات التحويلات والتكلفة/الكمية وربطها بالسجلات الفعلية.
- محاذاة نموذج Gemini الافتراضي في خدمة الذكاء الاصطناعي مع إعداد النشر الحالي.

## 2. فحوصات ناجحة
| الفحص | النتيجة |
|---|---|
| تحليل syntax لكل ملفات TS/TSX | **PASS — 228 ملفًا، 0 أخطاء parse** |
| فحص الاستيرادات النسبية مع دعم `.js -> .ts/.tsx` | **PASS — 230 ملفًا مفحوصًا، 0 مكسور** |
| سلامة الترجمة | **PASS — 1112 مفتاح rawUi فريد** |
| JSON: package/tsconfig/firebase/vercel/indexes/applet config | **PASS** |
| YAML: cloudbuild/render | **PASS** |
| API handlers المصدّرة الخاضعة للفحص | **PASS — بدون handler حساس بلا authenticateTrustedUser** |
| كتابات العميل إلى المجموعات المالية authoritative | **PASS — 0 حالات** |
| `dangerouslySetInnerHTML` / `innerHTML=` / `eval` / `new Function` | **PASS — 0 حالات** |
| Vercel catch-all API | **PASS — الملف موجود** |
| فهارس Firestore | **PASS — 26 index entries** |

## 3. اختبار TypeScript الفعلي
تم تشغيل `tsc --noEmit --pretty false`، لكن بيئة العمل الحالية لا تحتوي على `node_modules` ولم تنجح محاولة تثبيت الاعتماديات بسبب عدم توفر الحزم/الكاش محليًا والمهلة الشبكية.

الـdiagnostics الظاهرة كانت من نوع الاعتماديات المفقودة:
- TS2307: 306
- TS2339: 4 (مرتبطة بأنواع React المفقودة محليًا)
- TS2580: 73 (Node/process types غير مثبتة)
- TS2875: 92 (JSX runtime types غير مثبتة)

بعد عزل أخطاء البيئة/الاعتماديات، لا توجد أخطاء type mismatch محلية مستقلة تم اكتشافها في جولة التحليل السابقة.

## 4. ما لم يمكن إثباته داخل هذه البيئة
لم يكن ممكنًا إجراء اختبار runtime كامل للأجزاء التالية لأن الحزم والأدوات التشغيلية غير مثبتة محليًا:
- `npm run build`
- Vitest / Supertest
- Firebase Firestore/Storage emulator
- اختبار end-to-end حقيقي مع خدمات Firebase/Gemini

هذا لا يعني أن هذه الاختبارات فاشلة؛ بل يعني أنها **غير منفذة** في البيئة الحالية.

## 5. خطوة التحقق الخارجية المطلوبة قبل الإنتاج
بعد وضع المشروع في بيئة لديها وصول إلى سجل الحزم:
```bash
npm ci
npm run ci:check
```
ثم تشغيل اختبارات Firebase emulator الخاصة بالقواعد والتدفقات المالية/المخزون، واختبار build/deployment على Vercel أو Render باستخدام متغيرات البيئة الحقيقية.

يوصى أيضًا بإضافة `package-lock.json` مُثبت إلى مستودع الإنتاج بعد أول تثبيت ناجح حتى تصبح الإصدارات reproducible.

## 6. الملفات الجديدة
- `api/[...path].ts` — نقطة Vercel لتمرير طلبات API إلى Express.
- `Dockerfile` — إعداد حاوية تشغيل موحّد.

## 7. ملاحظة النسخة النهائية
هذه النسخة مبنية من الأرشيف المرفوع نفسه، مع بقاء الملفات الأصلية غير المتغيرة، وإدخال الإصلاحات المذكورة أعلاه فقط ضمن نسخة العمل النهائية.
